import React from 'react';

// interface BasicSelectorProps {
// }

export const BasicSelector = () => {
    return (
        <div>
            <select>
                <option></option>
            </select>
        </div>
    );
};
